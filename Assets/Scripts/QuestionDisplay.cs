using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class QuestionDisplay : MonoBehaviour
{
    public GameObject screenQuestion;
    public GameObject answerA;
    public GameObject answerB;
    public GameObject answerC;
    public GameObject answerD;

    void Start()
    {
        // screenQuestion.GetComponent<Text>().text = "How much wood could a wood chuck could chuck wood?";
        // answerA.GetComponent<Text>().text = "A. Lots and Lots";
        // answerA.GetComponent<Text>().text = "B. None";
        // answerA.GetComponent<Text>().text = "C. Not Much at all";
        // answerA.GetComponent<Text>().text = "D. 22";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
